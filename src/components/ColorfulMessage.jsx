import React from "react";

const ColorfulMessage = () => {
  const contentStyle = {
    color: "blue",
    fontSize: "18px"
  };

  return <p style={contentStyle}>おげんきですか</p>;
};

export default ColorfulMessage;
